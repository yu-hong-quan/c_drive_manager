[package]
name = "c_drive_manager_scanner"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
c_drive_manager_protocol = { path = "../protocol" }
serde = { version = "1", features = ["derive"] }
