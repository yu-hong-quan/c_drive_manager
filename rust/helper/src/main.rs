//! Elevated helper process entry point.
//!
//! The helper must only accept authenticated, structured IPC commands from the
//! desktop client. It must never expose a generic shell-command surface.

fn main() {
    println!("c_manager_helper placeholder");
}
