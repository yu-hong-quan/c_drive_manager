[package]
name = "c_drive_manager_helper"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[[bin]]
# The helper is an on-demand elevated executable, not a resident Windows service.
name = "c_manager_helper"
path = "src/main.rs"

[dependencies]
c_drive_manager_protocol = { path = "../protocol" }
