//! c_drive_manager_core crate placeholder.
//!
//! This module is intentionally narrow while the project skeleton settles. Add
//! public APIs here only after the shared schemas define their cross-language contract.

/// Returns the crate identifier for early workspace smoke checks.
pub fn crate_id() -> &'static str {
    "c_drive_manager_core"
}
