[package]
name = "c_drive_manager_ffi"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[lib]
# Flutter loads this crate as a Windows DLL through Dart FFI.
crate-type = ["cdylib"]
path = "src/lib.rs"

[dependencies]
c_drive_manager_protocol = { path = "../protocol" }
