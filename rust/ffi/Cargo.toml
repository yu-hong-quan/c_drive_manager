[package]
name = "c_drive_manager_ffi"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]
path = "src/lib.rs"

[dependencies]
c_drive_manager_core = { path = "../core" }
c_drive_manager_protocol = { path = "../protocol" }
