# Desktop Flutter App

Flutter Windows client for C 盘管家. This module owns presentation, navigation, task orchestration, local SQLite access, and FFI adapters. It must not directly delete system files or perform privileged Windows operations.
